# Database - Supabase PostgreSQL
# Get connection string from: Supabase Dashboard → Settings → Database → Connection string (URI)
# Replace [YOUR-PASSWORD] with your actual database password
# For connection pooling (recommended): use port 6543 instead of 5432
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres?pgbouncer=true&connection_limit=1"

# Alternative: Direct connection (without pooling)
# DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres"

NEXTAUTH_SECRET="generer-une-cle-secrete-longue-et-aleatoire"
NEXTAUTH_URL="https://votre-domaine.vercel.app"
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="changez-moi"

# Optionnel : stockage fichiers
# Option 1: Vercel Blob (créer un Blob Store dans Vercel > Storage)
BLOB_READ_WRITE_TOKEN="vercel_blob_..."

# Option 2: Cloudinary (alternative à Vercel Blob)
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"
CLOUDINARY_UPLOAD_PRESET="default"  # Optionnel
